version: "2"
linters:
  default: standard
  enable:
    - perfsprint
    - gocritic
    - nlreturn
    - misspell
    - whitespace
    - copyloopvar
    - usetesting
    - unconvert
    - bodyclose
    - wastedassign
    - nakedret
    - dogsled
    - nestif
    - mirror
    - reassign
    - fatcontext
    - durationcheck
    - makezero
    - usestdlibvars
    - testableexamples
    - intrange
    - thelper
    - unparam
    - predeclared
    - errname
    - prealloc
    - contextcheck
    - nilerr
    - forcetypeassert
    - noctx
    - wsl_v5
    - godot
    - gosec
    - goconst
    - revive
    - lll
    - funlen
    - errorlint
    - modernize
  settings:
    # Accepted-risk classes for this local CLI tool:
    #   G204: subprocesses always use the fixed "git"/"docker" binary; arguments
    #         come from internal callers (git output / local config), never
    #         from remote or untrusted user input.
    #   G304: reading files whose paths come from git output (repo-relative
    #         paths) or the CLI's own resolved config locations.
    #   G301: intentionally permissive (0755) fixture directories in tests.
    gosec:
      excludes:
        - G204
        - G304
        - G301
    funlen:
      lines: 65
      statements: 40
    lll:
      line-length: 120
    revive:
      rules:
        - name: package-comments
          disabled: false
        - name: context-as-argument
          disabled: false
        - name: unused-parameter
          disabled: false
    varnamelen:
      min-name-length: 2
      ignore-decls:
        - "t: testing.T"
        - "tt: *testing.T"
        - "i: int"
  exclusions:
    # Exclude machine-generated Go files (detected via standard "Code generated"
    # markers) from linting. Replaces an invalid v1-style exclusions.generate
    # block that golangci-lint v2 silently ignored.
    generated: lax
    rules:
      - linters: [funlen]
        path: "_test\\.go"
      - linters: [goconst]
        path: "_test\\.go"
formatters:
  enable:
    - gofmt
    - goimports
